// "use client";
import React from "react";
// import React, { useContext } from "react";
// import { ProductsContext } from "@/context/RootContext";

// ---------------------------------function start ------------------------------
export default function Home() {
  //   // Use context
  //   const context = useContext(ProductsContext);

  //   // Handle undefined context to ensure context is not null before Destructuring
  //   if (!context) {
  //     throw new Error(
  //       "products must be used within a RootContext provider"
  //     );
  //   }

  //   // Destructure after ensuring it's not null
  //   const { products} = context;

  return (
    <>
      <div>Home</div>
    </>
  );
}
